def agent_search(query: str, search_type: str = "semantic") -> list:
    """
    Orchestrates the search process between captioning and search modules.
    search_type: 'keyword' or 'semantic'
    Returns a list of matching image URLs.
    """
    # TODO: Implement agent logic
    return []
